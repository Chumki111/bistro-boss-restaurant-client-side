import PropTypes from 'prop-types';

const MenuItem = ({menu}) => {
    return (
        <div>
            
        </div>
    );
};
MenuItem.propTypes ={
    menu : PropTypes.menu
}
export default MenuItem;